services:
  - type: web
    name: ruby-wings-chatbot
    env: python
    plan: free

    buildCommand: "pip install --no-cache-dir -r requirements.txt"
    startCommand: "gunicorn app:app --bind 0.0.0.0:$PORT --workers 1 --threads 4 --timeout 300"

    envVars:
      # ===== Core =====
      - key: OPENAI_API_KEY
        sync: false  # Đặt trong Render Dashboard

      - key: OPENAI_BASE_URL
        value: "https://api.openai.com/v1"

      - key: FLASK_ENV
        value: "production"

      # ===== FAISS / Chatbot =====
      - key: FAISS_ENABLED
        value: "true"

      - key: RBW_ALLOW_REINDEX
        value: "0"

      - key: KNOWLEDGE_PATH
        value: "knowledge.json"

      - key: FALLBACK_VECTORS_PATH
        value: "vectors.npz"

      - key: FAISS_MAPPING_PATH
        value: "faiss_mapping.json"

      - key: FAISS_INDEX_PATH
        value: "faiss_index.bin"

      # ===== Embedding Models =====
      - key: EMBEDDING_MODEL
        value: "text-embedding-3-small"

      - key: CHAT_MODEL
        value: "gpt-4o-mini"

      - key: TOP_K
        value: "5"

      # ===== Google Sheets Integration =====
      - key: ENABLE_GOOGLE_SHEETS
        value: "true"

      - key: GOOGLE_SERVICE_ACCOUNT_JSON
        sync: false  # Đặt trong Render Dashboard với JSON đầy đủ

      - key: GOOGLE_SHEET_ID
        value: "1SdVbwkuxb8l1meEW--ddyfh4WmUvSXXMOPQ5bCyPkdk"

      - key: GOOGLE_SHEET_NAME
        value: "RBW_Lead_Raw_Inbox"

      # ===== Fallback Storage =====
      - key: ENABLE_FALLBACK_STORAGE
        value: "true"

      - key: FALLBACK_STORAGE_PATH
        value: "leads_fallback.json"

      # ===== Performance =====
      - key: WORKERS
        value: "1"

      - key: TIMEOUT
        value: "300"

      - key: DEBUG
        value: "false"

      # ===== Meta CAPI & Tracking =====
      - key: META_PIXEL_ID
        value: "862531473384426"

      - key: META_CAPI_TOKEN
        sync: false  # Đặt trong Render Dashboard

      - key: META_TEST_EVENT_CODE
        value: ""  # Để trống hoặc đặt mã test khi cần

      - key: ENABLE_META_CAPI_LEAD
        value: "true"  # Bật tracking lead qua /api/save-lead

      - key: ENABLE_META_CAPI_CALL
        value: "true"  # Bật tracking cho nút gọi điện & tư vấn

      # LƯU Ý: ĐÃ LOẠI BỎ GOOGLE ADS CONVERSION (chỉ dùng GA4)

    healthCheckPath: "/api/health"
    healthCheckTimeout: 30

    autoDeploy: true
    branch: main

    disk:
      name: chatbot-storage
      mountPath: /data
      sizeGB: 1

    # Add build cache for faster deployments
    buildFilter:
      paths:
        - "*.py"
        - "requirements.txt"
        - "knowledge.json"
        - "*.json"
      ignoredPaths:
        - "__pycache__/"
        - "*.pyc"
        - ".git/"